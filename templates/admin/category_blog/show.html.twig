{% extends 'admin.html.twig' %}

{% block title %}CategoryBlog{% endblock %}

{% block body %}
    <h1>CategoryBlog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_blog.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ category_blog.Titre }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ category_blog.Slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category_blog_index') }}">back to list</a>

    <a href="{{ path('app_admin_category_blog_edit', {'id': category_blog.id}) }}">edit</a>

    {{ include('admin/category_blog/_delete_form.html.twig') }}
{% endblock %}
