<form method="post" action="{{ path('app_admin_bonus_product_delete', {'id': bonus_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bonus_product.id) }}">
    <button class="btn">Delete</button>
</form>
