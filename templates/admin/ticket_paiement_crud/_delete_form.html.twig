<form method="post" action="{{ path('app_admin_ticket_paiement_c_r_u_d_delete', {'id': ticket_paiement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ticket_paiement.id) }}">
    <button class="btn">Delete</button>
</form>
