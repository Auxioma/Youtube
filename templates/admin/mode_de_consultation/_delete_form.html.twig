<form method="post" action="{{ path('app_admin_mode_de_consultation_delete', {'id': mode_de_consultation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_de_consultation.id) }}">
    <button class="btn">Delete</button>
</form>
