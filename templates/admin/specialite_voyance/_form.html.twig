
<div class="col-sm-12 col-xl-12">
    <div class="bg-light rounded h-100 p-4">
        <h6 class="mb-4"><a href="{{ path('app_admin_slider_index') }}">back to list</a></h6>
        {{ form_start(form) }}
        <div class="row mb-3">
            <label for="inputEmail3" class="col-sm-2 col-form-label">Title</label>
            <div class="col-sm-10">
                {{ form_widget(form.Titre) }}
            </div>
        </div>
        <div class="row mb-3">
            <label for="product_PriceHT" class="col-sm-2 col-form-label">Icone</label>
            <div class="col-sm-10">
                {{ form_widget(form.imageFile) }}
            </div>
        </div>
        <button type="submit" class="btn btn-primary">{{ button_label|default('Enregistré') }}</button>
        {{ form_end(form) }}
    </div>
</div>


