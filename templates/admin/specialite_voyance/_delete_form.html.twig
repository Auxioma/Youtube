<form method="post" action="{{ path('app_admin_specialite_voyance_delete', {'id': specialite_voyance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialite_voyance.id) }}">
    <button class="btn">Delete</button>
</form>
